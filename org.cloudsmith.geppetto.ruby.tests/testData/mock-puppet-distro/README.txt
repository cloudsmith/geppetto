This directory mimics a puppet distro layout.
A directory in the path is the version label.
A directory called "puppet" contains the directories "parser" and "type".

The testdata in this mock-distro contains very simple samples of functions and types.